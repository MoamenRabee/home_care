import 'package:flutter/material.dart';

class UserNewsScreen extends StatelessWidget {
  const UserNewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("الآخبار"),),
    );
  }
}